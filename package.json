{
  "name": "ci-artifacts-lab",
  "version": "1.0.0",
  "description": "CI artifacts investigation lab",
  "main": "src/app.js",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "jest --reporters=default --reporters=jest-junit"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.0",
    "jest-junit": "^16.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "jest-junit": {
    "outputDirectory": "./test-results",
    "outputName": "junit.xml"
  }
}
